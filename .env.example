# Server Configuration
HOST=0.0.0.0
PORT=8000

# Service Configuration
SERVICE_NAME=claude-code-sdk
LOG_LEVEL=info

# Command Configuration
COMMAND_TIMEOUT=300

# Note: Claude Code uses OAuth authentication
# No API key required - you'll login via: docker exec -it claude-sdk-agent claude login
