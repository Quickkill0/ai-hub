# Server Configuration
HOST=0.0.0.0
PORT=8000

# Service Configuration
SERVICE_NAME=ai-hub
LOG_LEVEL=info

# Command Configuration
COMMAND_TIMEOUT=300

# User/Group ID Configuration
# For Unraid, use PUID=99 and PGID=100
# For Docker Desktop or standard Linux, use PUID=1000 and PGID=1000
PUID=1000
PGID=1000

# Workspace directory (where projects are stored)
WORKSPACE_DIR=./workspace

# Note: Claude Code uses OAuth authentication
# No API key required - you'll login via: docker exec -it ai-hub claude login
